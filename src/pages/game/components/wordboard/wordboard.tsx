import { Box } from "grommet";

const Wordboard = (word: string, guesses: string[], currentGuess: string) => {
  const currentGuessIndex = guesses.indexOf("");
  return (
    <Box>
      {/* {guesses.map((guess, idx) => {
        if (guess)
      })} */}
    </Box>
  );
};

export default Wordboard;
